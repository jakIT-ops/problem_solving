
impl Solution {
    pub fn get_concatenation(nums: Vec<i32>) -> Vec<i32> {
        // nums.repeat(2) // method case
        // nums.extend(nums); // extend func
        let mut vec: Vec<i32> = Vec::with_capacity(2 * nums.len());
        vec.extend(&nums);
        vec.extend(&nums);

        // for &val in &nums {
        //     vec.push(val);
        // }
        // for &val in &nums {
        //     vec.push(val);
        // }
        return vec;
    }
}


